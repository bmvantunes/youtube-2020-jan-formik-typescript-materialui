import { Container } from '@material-ui/core';
import React from 'react';
import { FormDemo } from './FormDemo';

const App: React.FC = () => {
  return (
    <Container>
      <FormDemo />
    </Container>
  );
};

export default App;
